# 项目规格:商品库存 / 进销存管理系统(WMS-lite)

> 用途:放进目标仓库根目录作为 `CLAUDE.md`(所有 agent 的共享上下文)。
> 每个 agent 在动手前都必须先读本文件,遵守其中的领域模型、架构约定和硬性约束。

---

## 1. 项目目标

构建一套完整的商品库存管理系统,核心能力:
- **出入库**:入库(收货)、出库(发货)、库内调拨、库存盘点/调整
- **生产日期 / 批次管理**:按批次(生产日期、有效期)追踪库存,支持临期预警与先进先出(FEFO)
- **价格调整**:价目维护、批量调价、调价历史与生效日期管理
- **企业系统互联**:与内部系统(ERP、财务、身份认证等)通过既有端口互联互通
- **美观的 Web UI**:基于成熟开源方案,不从零手搓样式

## 2. 部署时配置(集成走配置驱动,不阻塞构建)

本项目采用**最大化兼容的可插拔集成框架**(见 §6):集成层对协议、认证、数据格式保持无关。因此下列信息**不再是构建前必须敲定的架构决策**,而是每个部署环境的运行时配置。agent 照 §6 把框架和参考适配器建好即可,无需等这些信息齐全。

仅以下两项建议构建前先定(非集成、影响全局):后端技术栈(对齐企业现状则更好)、数据库(默认 PostgreSQL)。

运行时配置(每部署环境一份,非构建阻塞项):

| 配置项 | 形态 | 说明 |
|---|---|---|
| 内部系统清单 | 配置文件 | 用到哪个配哪个 |
| 对接协议 | 选已支持适配器 | 见 §6 传输适配器 |
| 认证方式 | 选已支持适配器 | 见 §6 认证适配器 |
| 主数据来源与同步方向 | 配置 | 谁是 source of truth、入/出/双向 |
| 端点地址 | 配置 | **默认指向沙箱,切生产需显式确认** |
| 密钥 | vault / env | 不入库、不硬编码 |

## 3. 技术栈(默认选型,可对齐企业现状后推翻)

- 后端:**TypeScript + NestJS**(或 Python + FastAPI),REST API
- 数据库:**PostgreSQL** + 迁移工具(Prisma / TypeORM / Alembic)
- 前端:**React + TypeScript + Vite**,UI 基于成熟开源 admin 模板(见 §7)
- 部署:Docker + docker-compose(本地一键起全栈)
- 仓库结构:monorepo(`/backend`、`/frontend`、`/integrations`、`/docs`)

## 4. 领域模型(核心实体)

- **Product(商品/SKU)**:编码、名称、类别、单位、条码、规格属性
- **Warehouse / Location(仓库/库位)**:仓库、库区、货位
- **Batch(批次)**:批次号、所属商品、**生产日期、有效期**、状态 —— 生产日期管理的核心
- **StockMovement(库存流水)**:类型(入库 / 出库 / 调拨 / 盘点调整)、商品、批次、来源/目标库位、数量、时间、操作人、单据引用、原因
- **InventoryBalance(库存余额)**:商品 × 库位 × 批次 → 现存量(由流水推导或事务维护)
- **PriceList / ProductPrice(价目)**:价目表、商品价格、生效起止日期
- **PriceChangeLog(调价记录)**:调价前/后价格、生效日、操作人、原因(审计用)
- **User / Role / Permission(用户/角色/权限)**
- **AuditLog(审计日志)**:所有库存与价格变更的 who/when/before/after
- **IntegrationEndpoint / SyncJob(集成端点/同步任务)**

## 5. 核心功能模块

1. **入库**:收货单 → 选/建批次(录生产日期、有效期)→ 生成入库流水 → 更新余额
2. **出库**:发货单 → 按 FEFO 建议批次(临期优先出)→ 校验库存充足 → 生成出库流水
3. **调拨 / 盘点**:库位间转移;盘点差异生成调整流水
4. **批次 / 生产日期管理**:批次追踪、临期报表、过期拦截、FEFO/FIFO 策略可配
5. **价格调整**:单品/批量调价、生效日期、调价历史;可选审批流
6. **库存查询与报表**:实时库存、流水明细、临期/过期、库存估值
7. **集成**:从内部系统同步主数据、向财务推送库存/价值变动、SSO 登录

## 6. 集成层:最大化兼容(可插拔框架 + 配置驱动)

**核心原则**:业务逻辑只依赖内部**规范模型(canonical model)**;所有外部系统的差异都封在可插拔适配器 + 配置里。新增一个已支持协议类型的系统 = 改配置,不改代码。

**防腐层(ACL)**:每个外部系统的字段、编码、单位、日期格式差异都在 ACL 里翻译成规范模型,业务层永远看不到外部脏细节。

**传输适配器(可插拔,统一 Connector 接口)**:
- HTTP/REST(JSON)、SOAP/XML(WSDL)、GraphQL、gRPC
- 消息队列:AMQP/RabbitMQ、Kafka、MQTT、JMS
- 数据库直连:只读副本 / 视图,**禁止直写外部生产库**
- 文件:CSV / Excel / XML / 固定宽度 / EDI,经 SFTP 或共享目录
- 入站 webhook 接收

**认证适配器(可插拔)**:
- API Key / Bearer Token、OAuth2(client credentials / auth code)、OIDC、Basic、mTLS 客户端证书、HMAC 签名、SAML(用户 SSO)、IP allowlist

**格式与映射**:
- 解析/序列化:JSON、XML、CSV、固定宽度
- 字段映射配置(external ↔ canonical)、编码字典映射、单位/日期转换、schema 校验

**框架核心**:
- Connector 插件注册与接口契约(新适配器实现接口即可接入)
- 配置驱动端点:已支持协议下,新增端点只改配置
- 每端点:同步方向(入/出/双向)、触发(实时 webhook / 轮询 / 定时批)、环境(**默认沙箱**,切生产需显式确认)
- 可靠性:幂等键、指数退避重试、死信队列、熔断
- 密钥:抽象为 vault / env,可替换
- 可观测:每端点同步状态、日志、指标、失败告警
- 契约先行:对接前先出接口契约文档(请求/响应/错误码)

**优先用标准降低对接成本**:REST 出 OpenAPI、SOAP 认 WSDL;商品/条码对齐 GS1/GTIN 等通用标准。

**⚠️ 避免过度工程(关键,直接关系到 token 成本)**:"最大化兼容" = 把**接缝**做成可插拔 + 配置驱动,不是第一天就把十种协议全实现完。先交付框架 + 少量参考适配器(REST + 一种消息队列 + 文件),其余协议/认证方式作为**有文档的扩展点**,真正用到再补。写一堆没人用的适配器纯烧钱。

## 7. UI 方向(调研现有开源方案,不手搓)

- 先在 GitHub 调研成熟开源 admin / dashboard 方案,按以下标准选型并记录理由:
  - 许可证允许商用(MIT/Apache 优先,**逐一核对 license**)
  - 与所选前端栈契合、活跃维护、组件覆盖(表格/表单/图表/权限布局)
  - 中文场景友好
- 候选方向示例(agent 自行调研当前最优,不限于此):基于 shadcn/ui 的 admin 模板、Refine、Ant Design Pro、Tremor 等
- 选定后:抽取设计规范(配色、字体、间距、组件),沉淀为前端设计 token,后续页面统一复用,避免每个 agent 各画各的

## 8. 横切要求(每个 PR 都要满足)

- **并发安全**:库存扣减必须事务化 + 行锁或乐观并发控制,杜绝超卖/负库存
- **RBAC**:接口按角色鉴权
- **审计**:每一次库存与价格变更写审计日志(不可绕过)
- **测试**:每个 PR 附单元测试;涉及流程的附集成测试;CI 全绿才合并
- **i18n**:中文为主
- **可观测**:结构化日志,关键操作可追踪

## 9. 所有 agent 的通用约定

- 动手前先读本文件 + 现有代码,不重复造轮子、不偏离既有约定
- 严格待在分配给你的模块/目录边界内,越界改动先上报
- 密钥走环境变量,绝不硬编码或提交
- 数据库变更用迁移,不手改 schema
- API 变更同步更新 `/docs` 下的接口文档
- 提交信息清晰,PR 描述含:任务、改动摘要、测试说明、关联任务号

## 10. 任务分解(按波次;地基波先串行落地,再并行)

**Wave 0 — 地基(串行,必须先合并)**
- T0.1 调研 & 定方案:核对 §2 必填项,产出 `ARCHITECTURE.md` 与 UI 选型结论;缺信息则列为待确认并暂停依赖任务
- T0.2 脚手架:monorepo、CI、lint、测试框架、docker-compose 一键起栈
- T0.3 数据库 schema + 迁移(§4 全实体)+ 种子数据
- T0.4 认证 / RBAC + 审计日志骨架

**Wave 1 — 核心业务(并行,依赖 Wave 0)**
- T1.1 商品/SKU 主数据 CRUD(API + 测试)
- T1.2 仓库/库位 CRUD
- T1.3 批次模型 + 生产日期/有效期/临期逻辑
- T1.4 入库流程
- T1.5 出库流程 + FEFO 建议
- T1.6 调拨 + 盘点/库存调整
- T1.7 库存余额查询 & 报表 API
- T1.8 价目 + 调价(历史 + 生效日期)
- T1.9 UI 地基:落地选定的 admin 模板 + 布局 + 登录页

**Wave 2 — UI 页面 & 集成(并行,依赖 Wave 1)**
- T2.1~ 各模块前端页面(库存 / 批次临期 / 调价 / 报表看板)
- T2.5~ 集成连接器:主数据同步、财务推送、SSO —— 每个走 adapter + 沙箱配置 + 契约文档
- T2.9 临期预警 / 仪表盘

**Wave 3 — 收尾**
- 端到端测试、并发/超卖压力测试、性能与安全加固、部署与运维文档

## 11. 给流水线的运行建议

- 按波次跑:先跑 Wave 0 全部并确认合并,再放 Wave 1,依此类推 —— 别一次性把全部任务丢进去
- Wave 0 的 T0.1 建议**用较强模型 / 人工过一遍**,因为它定架构,错了后面全歪
- 集成类任务(T2.5+)因涉及真实端口,建议单独低并发跑并重点盯输出
